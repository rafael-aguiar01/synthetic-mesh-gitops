apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: modulo-observabilidade
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/rafael-aguiar01/synthetic-mesh-gitops.git'
    targetRevision: HEAD
    path: observabilidade
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: observabilidade
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true