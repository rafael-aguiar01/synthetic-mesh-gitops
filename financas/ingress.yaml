apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
    - host: iptu.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tributos-iptu-dummy-service
                port:
                  number: 80