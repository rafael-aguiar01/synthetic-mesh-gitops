apiVersion: v1
kind: ConfigMap
metadata:
  name: portal-do-cidadao-code
  namespace: platform
data:
  index.js: |
    const http = require('http');
    const port = 7007;
    const server = http.createServer((req, res) => {
      console.log("Requisicao recebida no modulo: portal-do-cidadao");
      res.writeHead(200, { 'Content-Type': 'application/json' });
      res.end(JSON.stringify({ status: "UP", module: "portal-do-cidadao" }));
    });
    server.listen(port, () => {
      console.log("Servico portal-do-cidadao rodando na porta " + port);
    });