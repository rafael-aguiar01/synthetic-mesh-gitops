apiVersion: apps/v1
kind: Deployment
metadata:
  name: portal-do-cidadao
  namespace: platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portal-do-cidadao
  template:
    metadata:
      labels:
        app: portal-do-cidadao
    spec:
      containers:
        - name: app
          image: node:18-alpine
          command: ["node", "/app/index.js"]
          ports:
            - containerPort: 7007
          volumeMounts:
            - name: code-volume
              mountPath: /app
      volumes:
        - name: code-volume
          configMap:
            name: portal-do-cidadao-code