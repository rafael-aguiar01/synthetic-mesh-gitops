apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${{ values.name }}
  namespace: platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${{ values.name }}
  template:
    metadata:
      labels:
        app: ${{ values.name }}
    spec:
      containers:
        - name: app
          image: node:18-alpine
          command: ["node", "/app/index.js"]
          ports:
            - containerPort: 7007
          volumeMounts:
            - name: code-volume
              mountPath: /app
      volumes:
        - name: code-volume
          configMap:
            name: ${{ values.name }}-code