apiVersion: apps/v1
kind: Deployment
metadata:
  name: saudepublica
  namespace: platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: saudepublica
  template:
    metadata:
      labels:
        app: saudepublica
    spec:
      containers:
        - name: app
          image: node:18-alpine
          command: ["node", "/app/index.js"]
          ports:
            - containerPort: 7007
          volumeMounts:
            - name: code-volume
              mountPath: /app
      volumes:
        - name: code-volume
          configMap:
            name: saudepublica-code